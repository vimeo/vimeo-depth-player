<script src="/js/gltfloader.js"></script>

<script>
  const modelLoader = new THREE.GLTFLoader();
  modelLoader.load('/assets/brooklyn.glb', obj=>{

    const groupedModelElements = obj.scene.children[0];
    groupedModelElements.position.y -= 0.3;
    glScene.scene.add(groupedModelElements);

  });
</script>
